FROM mcr.microsoft.com/playwright:v1.51.1-jammy

WORKDIR /app
COPY . .

# Phase 2 will replace this with Chrome + sidecar runtime wiring.
CMD ["/bin/bash", "-lc", "echo browser node image placeholder; sleep infinity"]
